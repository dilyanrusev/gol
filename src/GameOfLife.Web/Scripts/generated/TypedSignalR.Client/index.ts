/* THIS (.ts) FILE IS GENERATED BY TypedSignalR.Client.TypeScript */
/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import type { HubConnection, IStreamResult, Subject } from '@microsoft/signalr';
import type { ILifeHub, ILifeClient } from './GameOfLife.Web.Hubs';
import type { Frame } from '../GameOfLife.Web.Simulation';


// components

export type Disposable = {
    dispose(): void;
}

export type HubProxyFactory<T> = {
    createHubProxy(connection: HubConnection): T;
}

export type ReceiverRegister<T> = {
    register(connection: HubConnection, receiver: T): Disposable;
}

type ReceiverMethod = {
    methodName: string,
    method: (...args: any[]) => void
}

class ReceiverMethodSubscription implements Disposable {

    public constructor(
        private connection: HubConnection,
        private receiverMethod: ReceiverMethod[]) {
    }

    public readonly dispose = () => {
        for (const it of this.receiverMethod) {
            this.connection.off(it.methodName, it.method);
        }
    }
}

// API

export type HubProxyFactoryProvider = {
    (hubType: "ILifeHub"): HubProxyFactory<ILifeHub>;
}

export const getHubProxyFactory = ((hubType: string) => {
    if(hubType === "ILifeHub") {
        return ILifeHub_HubProxyFactory.Instance;
    }
}) as HubProxyFactoryProvider;

export type ReceiverRegisterProvider = {
    (receiverType: "ILifeClient"): ReceiverRegister<ILifeClient>;
}

export const getReceiverRegister = ((receiverType: string) => {
    if(receiverType === "ILifeClient") {
        return ILifeClient_Binder.Instance;
    }
}) as ReceiverRegisterProvider;

// HubProxy

class ILifeHub_HubProxyFactory implements HubProxyFactory<ILifeHub> {
    public static Instance = new ILifeHub_HubProxyFactory();

    private constructor() {
    }

    public readonly createHubProxy = (connection: HubConnection): ILifeHub => {
        return new ILifeHub_HubProxy(connection);
    }
}

class ILifeHub_HubProxy implements ILifeHub {

    public constructor(private connection: HubConnection) {
    }

    public readonly pan = async (dx: number, dy: number): Promise<Frame> => {
        return await this.connection.invoke("Pan", dx, dy);
    }

    public readonly resize = async (width: number, height: number): Promise<Frame> => {
        return await this.connection.invoke("Resize", width, height);
    }

    public readonly recentre = async (): Promise<Frame> => {
        return await this.connection.invoke("Recentre");
    }

    public readonly refresh = async (): Promise<Frame> => {
        return await this.connection.invoke("Refresh");
    }

    public readonly start = async (): Promise<void> => {
        return await this.connection.invoke("Start");
    }

    public readonly pause = async (): Promise<void> => {
        return await this.connection.invoke("Pause");
    }

    public readonly step = async (): Promise<void> => {
        return await this.connection.invoke("Step");
    }

    public readonly reset = async (): Promise<void> => {
        return await this.connection.invoke("Reset");
    }

    public readonly setSpeed = async (generationsPerSecond: number): Promise<void> => {
        return await this.connection.invoke("SetSpeed", generationsPerSecond);
    }
}


// Receiver

class ILifeClient_Binder implements ReceiverRegister<ILifeClient> {

    public static Instance = new ILifeClient_Binder();

    private constructor() {
    }

    public readonly register = (connection: HubConnection, receiver: ILifeClient): Disposable => {

        const __receiveFrame = (...args: [Frame]) => receiver.receiveFrame(...args);

        connection.on("ReceiveFrame", __receiveFrame);

        const methodList: ReceiverMethod[] = [
            { methodName: "ReceiveFrame", method: __receiveFrame }
        ]

        return new ReceiverMethodSubscription(connection, methodList);
    }
}

